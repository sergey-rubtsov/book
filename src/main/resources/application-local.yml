spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:cookbook;INIT=CREATE SCHEMA IF NOT EXISTS cookbook;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  h2:
    console:
      enabled: true